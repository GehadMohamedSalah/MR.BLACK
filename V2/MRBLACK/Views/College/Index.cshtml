@model MRBLACK.Helper.PagedList<MRBLACK.Models.Database.College>
@using MRBLACK.Models.ViewModels;
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var paginationVM = new PaginationVM()
    {
        PageIndex = Model.PageIndex,
        TotalPages = Model.TotalPages,
        PreviousPage = Model.PreviousPage,
        NextPage = Model.NextPage,
        GetItemsUrl = "/College/GetItems",
        GetPaginationUrl = "/College/GetPagination",
        ItemsCount = Model.ItemsCount,
        StartIndex = Model.StartIndex,
        EndIndex = Model.EndIndex
    };
}
<button type="button" class="btn btn-buy" onclick="BackToPreviousPage()"><i class="fa fa-forward"></i> العودة للصفحة السابقة</button>

<h1 class="text-center">شاشة الكليات</h1>
<div class="panel panel-default">
    <div class="panel-heading">
        <div class="row">
            <div class="col-sm-6">
                <button type="button" class="btn btn-success" data-toggle="modal" data-target="#flipFlop" onclick="FillPopup('/College/Create')">
                    <i class="fa fa-plus"></i><span>  اضافة جديد</span>
                </button>
            </div>
            <partial name="_TableNavbar" model="@Model.PageIndex" />
        </div>


    </div>
    <div class="panel-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-bordered border-primary">
                <thead class="bg-blue">
                    <tr>
                        <td>#</td>
                        <td>الكود</td>
                        <td>الاسم بالانجليزية</td>
                        <td>الاسم بالعربية</td>
                        <td>الدول</td>
                        <td>الجامعات</td>
                        <td>الاقسام</td>
                        <td>المجموعات</td>
                        <td>المواد</td>
                        <td>السنين الدراسية</td>
                        <td>الترمات</td>
                        <td>الخدمات</td>
                        <td>الشعار</td>
                        <td>الاجراءات</td>
                    </tr>
                </thead>
                <tbody id="tbBody">
                    <partial name="_TableList" model="@Model.ToList()" />
                </tbody>
            </table>
            <div id="dvPagination">
                <partial name="_Pagination" model="@paginationVM" />
            </div>
        </div>

    </div>
</div>

